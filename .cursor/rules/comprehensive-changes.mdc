---
description: 一次性改全，禁止最小 patch / 半套实现
alwaysApply: true
---

# 一次性改全（禁止最小改动）

修改本仓库时，**不要**以「最小 diff」「先凑合能跑」为目标。要改就 **一次改到位**。

## 默认做法

- 功能涉及一个子系统（todo、UI、session、tools、hooks 等）时，**整条链路一起改**：数据模型 → 工具/handler → loop/prompt → 终端展示 → 与现有约定对齐。
- 发现相关技术债会妨碍本次功能正确性时，**同一轮一并清掉**，不要留「下次再补」。
- 结构不清晰时 **拆模块、重命名、收拢入口**，而不是在大文件里再贴 20 行。
- 改完应能 **独立运行验证**；不要交付「只改了一半、另一半靠用户猜」的状态。

## 禁止

- 只改一行、只加一个 print、只换字符串，而架构问题仍在。
- 「最小可行」式半套：例如加了 `todo_write` 却不注入 prompt、不刷新 UI、不补 tool 描述。
- 为省 diff 留下 mock / dead path 与正式路径混在一起且无边界。
- 在回复里写「如需完整版可以再改」——用户要的就是这一轮改全。

## 仍然遵守

- **范围对齐用户真实需求**：改全 ≠ 无关大重写。
- 匹配现有命名、async、tool dispatch 等惯例，除非本次任务就是要重构这些惯例。
- 用户没要的测试/文档可以不写，除非 **不写就无法正确完成** 本次改动。

## 示例

```text
# ❌ 最小改动
# todo.py  return 改成多打一行字，loop 和 prompts 不动

# ✅ 一次性改全
# todo.py      activeForm + 格式化返回 + 持久化
# prompts.py   每轮注入当前 todos
# registry.py  完整 tool description（何时用/不用/纪律）
# loop.py      按 LLM 回合计数 + reminder 带完整表
# ui/          checklist 渲染，todo_write 后刷新
```
